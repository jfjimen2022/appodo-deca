# URL base del backend Django/DRF de Appodo DeCa. En dev, déjalo vacío para
# usar el proxy de Vite (ver vite.config.js, apunta a http://127.0.0.1:8000).
# En producción, apunta al dominio donde sirvas el backend (sin barra final),
# por ejemplo: https://deca.tuempresa.com
VITE_API_URL=
